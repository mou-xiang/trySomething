# PLAN.md — 项目进度与打卡清单（习惯工具）

## 项目

**手写 Web 服务器 + 部署到腾讯云/阿里云（香港，免备案）**
目标：自己写的 Node http 服务器 + 自己的域名，公网 HTTPS 可访问；访客计数器 + 留言板。

## 每周固定时段（重要！像约会一样不可改期）

- 时段 1：________（如：周二 20:00）
- 时段 2：________（如：周六 20:00）

## 规则

- 最小有效会话：15 分钟也算打卡，但**不允许跳过**。
- 卡壳规则：同一 bug 超过 20 分钟 → 停下，描述现象求助，禁止硬磨。
- 收尾仪式：每次会话结束做一个 commit → 在下方"打卡记录"写一行。

## 当前会话目标（M4b：域名 + HTTPS）

1. 域名 A 记录指向服务器公网 IP（DNSPod 控制台）
2. 申请免费 DV 证书（certbot 或腾讯云 SSL）
3. 改造 server.js：https 服务监听 443（用户写代码，AI 示范模式）
4. 端口权限：非 root 绑定 80/443 需 setcap（或说明原理）
5. pm2 重启，https://你的域名 绿锁可访问
验收：浏览器 https://你的域名 看到网站、绿锁、计数+留言正常

（M4a ✅ 已完成 2026-08-20：Node 22 + pm2 守护 + http://IP:8080 可访问可留言）

## 里程碑清单

- [x] M0 初始化：最小服务器 + curl 验证（2026-08-16）
- [x] M1 静态文件服务 + 路由（/、/about、404）（2026-08-17）
- [x] M2 API 与数据：访客计数 + 留言板，JSON 持久化（2026-08-19）
- [x] M3 网络理论 + 买域名 + 买香港轻量服务器 + 实名（2026-08-20）
- [ ] M4 部署：SSH → Node → 防火墙 → pm2 → A 记录 → HTTPS
- [ ] M5 上线打磨：强制 HTTPS、看日志、公测发朋友

拉伸目标（M5 后）：缓存头 / HTTP/2 / 留言过滤 / uptime 监控 / Docker

## 打卡记录

| 日期 | 本次做了什么 | 结果 |
|------|--------------|------|
| 2026-08-16 | M0：写最小服务器，curl 验证 200 OK | ✅ |
| 2026-08-17 | M1：静态文件 + 路由 + serverFile 重构，4 条 curl 全绿 | ✅ |
| 2026-08-19 | M2：API 与数据，12 项测试全绿 + 持久化验证通过 | ✅ |
| 2026-08-19 | 复习：M2 六大概念自测 6/6 全对（JSON/流/GET-POST/异常/持久化/函数调用） | ✅ |
| 2026-08-20 | M3：网络理论（DNS/TCP/HTTP 实验）+ 购买腾讯云香港轻量（Debian）+ 域名已购实名通过 | ✅ |
| 2026-08-20 | M4a：服务器环境（Node 22 + pm2 守护）+ 网站 http://IP:8080 上线可留言 | ✅ |
