# PLAN.md — 项目进度与打卡清单（习惯工具）

## 项目

**手写 Web 服务器 + 部署到腾讯云/阿里云（香港，免备案）**
目标：自己写的 Node http 服务器 + 自己的域名，公网 HTTPS 可访问；访客计数器 + 留言板。

## 每周固定时段（重要！像约会一样不可改期）

- 时段 1：________（如：周二 20:00）
- 时段 2：________（如：周六 20:00）

## 规则

- 最小有效会话：15 分钟也算打卡，但**不允许跳过**。
- 卡壳规则：同一 bug 超过 20 分钟 → 停下，描述现象求助，禁止硬磨。
- 收尾仪式：每次会话结束做一个 commit → 在下方"打卡记录"写一行。

## 当前会话目标（M1 ✅ 已完成 2026-08-17）

完成：静态文件服务 + 路由（/、/about、404）+ serverFile 帮助函数重构
验收：4 条 curl 全绿（200/200/200/404），浏览器页面+样式正常

（M2 目标将在下次会话课前写入）

## 里程碑清单

- [x] M0 初始化：最小服务器 + curl 验证（2026-08-16）
- [x] M1 静态文件服务 + 路由（/、/about、404）（2026-08-17）
- [ ] M2 API 与数据：访客计数 + 留言板，JSON 持久化
- [ ] M3 网络理论 + 买域名 + 买香港轻量服务器 + 实名
- [ ] M4 部署：SSH → Node → 防火墙 → pm2 → A 记录 → HTTPS
- [ ] M5 上线打磨：强制 HTTPS、看日志、公测发朋友

拉伸目标（M5 后）：缓存头 / HTTP/2 / 留言过滤 / uptime 监控 / Docker

## 打卡记录

| 日期 | 本次做了什么 | 结果 |
|------|--------------|------|
| 2026-08-16 | M0：写最小服务器，curl 验证 200 OK | ✅ |
| 2026-08-17 | M1：静态文件 + 路由 + serverFile 重构，4 条 curl 全绿 | ✅ |
